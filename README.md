# Genetics Learning Hub

Nền tảng học tập Di truyền học tương tác trực tuyến.

## Hướng dẫn Deploy lên Render:

1. Đẩy toàn bộ thư mục code lên **GitHub Repository** của bạn:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin [https://github.com/your-username/genetics-learning-hub.git](https://github.com/your-username/genetics-learning-hub.git)
   git push -u origin main